# Copy this file to .env.local and fill in your Raspberry Pi's IP address.
# This is used by the CameraFeed component as the default IP.
# The user can also override it at runtime from the camera modal.
#
# Example: VITE_RPI_HOST=192.168.1.42
VITE_RPI_HOST=172.20.10.11
