# BME680 sensor reader

This folder contains a minimal Python reader for gathering data from the Adafruit BME680 sensor.

## install

1) Enable I2C on the Raspberry Pi (raspi-config).
2) Install dependencies:

- `pip install -r requirements.txt`

## run

- `python bme680_reader.py`
