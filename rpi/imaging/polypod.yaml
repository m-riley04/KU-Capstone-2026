# NOTE: structured off of the rpi-image-gen examples.

include:
  file: bookworm-minbase-ab.yaml

device:
  layer: rpi5
  hostname: polypod
  user1: pod
  user1pass: Fo0bar!!

image:
  system_part_size: 8000M  # Full Pi desktop needs ~5-6GB
  name: polypod

layer:
  app: mypolypod
  extra: rpi-splash-screen

splash:
  image_path: ${@SRCROOT}/splash.tga