# =============================================================================
# /etc/udev/rules.d/99-waveshare-touch.rules
# Ensure touch devices have correct permissions and symlinks
# =============================================================================

# Goodix GT911 capacitive touch (DSI E display)
SUBSYSTEM=="input", ATTRS{name}=="Goodix Capacitive TouchScreen", MODE="0666", SYMLINK+="input/polypod-touch-top"

# ADS7846 resistive touch (SPI G display)
SUBSYSTEM=="input", ATTRS{name}=="ADS7846 Touchscreen", MODE="0666", SYMLINK+="input/polypod-touch-bottom"

# Allow pi user access to DRM devices
SUBSYSTEM=="drm", MODE="0666"

# Allow pi user access to SPI devices
SUBSYSTEM=="spidev", MODE="0666"

# Allow pi user access to I2C devices
SUBSYSTEM=="i2c-dev", MODE="0666"
